/**
 * Implementation of Queue Abstract Data Structure
 *
 * Interface:
 *
 * 1. constructor: Queue(length)
 * 2. add(item)
 * 3. remove(item)
 * 4. length()
 * 5. isEmpty()
 * 6. isFull()
 * 7. front()
 *
 */

class Queue {
  constructor(length) {
    this.length = length;
    this._Queue = [];
  }

  add(item) {}
}
